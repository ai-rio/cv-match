#!/usr/bin/env sh

echo "📝 Validating commit message..."

# Skip commitlint for now (will enable after setup)
echo "📝 Commit message validation temporarily disabled"

echo "✅ Commit message validation passed!"