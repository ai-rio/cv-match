// This layout file is intentionally minimal
// The next-intl middleware handles locale-based routing
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
